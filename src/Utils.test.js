const assert = require("assert");
const _ = require("lodash");
const checkEnvironment = require("./Utils").checkEnvironment;

describe("Utils", function () {
  it("checkEnvironment", async function () {
    checkEnvironment([]);
  });
});
