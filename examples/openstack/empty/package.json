{
  "name": "example-grucloud-infra-openstack",
  "version": "1.29.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "graph-target": "gc graph",
    "graph-live": "gc list --graph --default-exclude --all --json gc-list.json",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.29.0",
    "@grucloud/provider-openstack": "^1.29.0",
    "rubico": "^1.6.29"
  }
}
