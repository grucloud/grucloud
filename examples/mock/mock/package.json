{
  "name": "grucloud-mock-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "e2e": "gc d -a -f && gc run --onDestroyed && gc d -a && gc l && gc a -f && gc a && gc run --onDeployed && gc d -f && gc d && gc l",
    "link": "npm link @grucloud/core"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "*"
  }
}
