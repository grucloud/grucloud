{
  "name": "@grucloud/example-mock-simple",
  "version": "11.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "e2e": "gc d -a -f && gc run --onDestroyed && gc d -a && gc l && gc a -f && gc a && gc run --onDeployed && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^11.0.0",
    "@grucloud/provider-mock": "^11.0.0",
    "url-join": "^4.0.1"
  },
  "devDependencies": {
    "mocha": "^9.0.3",
    "mocha-junit-reporter": "^2.0.2"
  }
}
