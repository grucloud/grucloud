{
  "name": "@grucloud/example-mock-simple",
  "version": "1.15.1",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "e2e": "gc d -a -f && gc run --onDestroyed && gc d -a && gc l && gc a -f && gc a && gc run --onDeployed && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.15.1",
    "@grucloud/provider-mock": "^1.15.1"
  }
}
