{
  "name": "@grucloud/example-aws-s3-simple",
  "version": "1.20.0",
  "private": true,
  "description": "S3 example",
  "main": "index.js",
  "scripts": {
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.20.0",
    "@grucloud/provider-aws": "^1.20.0"
  }
}
