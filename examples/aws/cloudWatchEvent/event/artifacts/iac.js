// Generated by aws2gc
const { get } = require("rubico");
const { AwsProvider } = require("@grucloud/provider-aws");

const createResources = ({ provider }) => {
  provider.CloudWatchEvents.makeEventBus({
    name: get("config.CloudWatchEvents.EventBus.busTest.name"),
  });

  provider.CloudWatchEvents.makeRule({
    name: get("config.CloudWatchEvents.Rule.ruleTest.name"),
    properties: get("config.CloudWatchEvents.Rule.ruleTest.properties"),
  });

  provider.CloudWatchEvents.makeRule({
    name: get("config.CloudWatchEvents.Rule.ruleTestEc2.name"),
    properties: get("config.CloudWatchEvents.Rule.ruleTestEc2.properties"),
    dependencies: ({ resources }) => ({
      eventBus: resources.CloudWatchEvents.EventBus.busTest,
    }),
  });
};

exports.createResources = createResources;

exports.createStack = async ({ createProvider }) => {
  const provider = createProvider(AwsProvider, { config: require("./config") });
  createResources({
    provider,
  });

  return {
    provider,
  };
};
