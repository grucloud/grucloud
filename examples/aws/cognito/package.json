{
  "name": "@grucloud/example-aws-cognito",
  "version": "1.39.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph --types-exclude KeyPair",
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.39.0",
    "@grucloud/provider-aws": "^1.39.0",
    "rubico": "^1.6.27"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
