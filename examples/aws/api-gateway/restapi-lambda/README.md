# REST API Gateway with Lambda

The purpose of this example is to deploy server a lambda function with the API Gateway

## Dependency Graph

```sh
gc graph
```

![GraphTarget](artifacts/diagram-target.svg)
