{
  "name": "@grucloud/example-aws-ec2-security-group",
  "version": "1.30.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "graph-target": "gc graph",
    "graph-live": "gc list --graph ",
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.29.0",
    "@grucloud/provider-aws": "^1.30.0",
    "rubico": "^1.6.27"
  }
}
