{
  "name": "example-grucloud-infra-aws",
  "version": "1.34.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.34.0",
    "@grucloud/provider-aws": "^1.34.0",
    "rubico": "^1.6.29"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
