// Generated by 'gc gencode'
const {} = require("rubico");
const {} = require("rubico/x");

exports.createResources = () => [
  {
    type: "Cluster",
    group: "DocDBElastic",
    properties: ({}) => ({
      adminUserName: "docdbuser",
      authType: "PLAIN_TEXT",
      clusterName: "my-cluster",
      preferredMaintenanceWindow: "Sun:09:48-Sun:10:18",
      shardCapacity: 2,
      shardCount: 2,
      adminUserPassword: process.env.MY_CLUSTER_ADMIN_USER_PASSWORD,
    }),
    dependencies: ({ config }) => ({
      subnets: [
        `my-vpc-vpc::my-vpc-subnet-private1-${config.region}a`,
        `my-vpc-vpc::my-vpc-subnet-private2-${config.region}b`,
      ],
      securityGroups: ["sg::my-vpc-vpc::default"],
    }),
  },
  {
    type: "RouteTable",
    group: "EC2",
    name: ({ config }) => `my-vpc-rtb-private1-${config.region}a`,
    dependencies: ({}) => ({
      vpc: "my-vpc-vpc",
    }),
  },
  {
    type: "RouteTable",
    group: "EC2",
    name: ({ config }) => `my-vpc-rtb-private2-${config.region}b`,
    dependencies: ({}) => ({
      vpc: "my-vpc-vpc",
    }),
  },
  {
    type: "RouteTableAssociation",
    group: "EC2",
    dependencies: ({ config }) => ({
      routeTable: `my-vpc-vpc::my-vpc-rtb-private1-${config.region}a`,
      subnet: `my-vpc-vpc::my-vpc-subnet-private1-${config.region}a`,
    }),
  },
  {
    type: "RouteTableAssociation",
    group: "EC2",
    dependencies: ({ config }) => ({
      routeTable: `my-vpc-vpc::my-vpc-rtb-private2-${config.region}b`,
      subnet: `my-vpc-vpc::my-vpc-subnet-private2-${config.region}b`,
    }),
  },
  {
    type: "SecurityGroup",
    group: "EC2",
    name: "sg::my-vpc-vpc::default",
    isDefault: true,
    dependencies: ({}) => ({
      vpc: "my-vpc-vpc",
    }),
  },
  {
    type: "Subnet",
    group: "EC2",
    name: ({ config }) => `my-vpc-subnet-private1-${config.region}a`,
    properties: ({ config }) => ({
      AvailabilityZone: `${config.region}a`,
      NewBits: 4,
      NetworkNumber: 8,
    }),
    dependencies: ({}) => ({
      vpc: "my-vpc-vpc",
    }),
  },
  {
    type: "Subnet",
    group: "EC2",
    name: ({ config }) => `my-vpc-subnet-private2-${config.region}b`,
    properties: ({ config }) => ({
      AvailabilityZone: `${config.region}b`,
      NewBits: 4,
      NetworkNumber: 9,
    }),
    dependencies: ({}) => ({
      vpc: "my-vpc-vpc",
    }),
  },
  {
    type: "Vpc",
    group: "EC2",
    name: "my-vpc-vpc",
    properties: ({}) => ({
      CidrBlock: "10.0.0.0/16",
      DnsHostnames: true,
    }),
  },
];
