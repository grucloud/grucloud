{
  "name": "@grucloud/docker-aws",
  "version": "1.15.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "docker:build": "docker build -t grucloud-aws .",
    "gc:list": "docker run --mount type=bind,source=$PWD/volume,target=/app/output --env-file=aws.env grucloud-aws list --json output/gc-list.json --graph --all --dot-file output/gc.dot",
    "gc:info": "docker run --env-file=aws.env grucloud-aws info",
    "gc:version": "docker run --env-file=aws.env grucloud-aws --version"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.15.0",
    "@grucloud/provider-aws": "^1.15.0"
  }
}
