{
  "name": "kms-symmetric",
  "version": "2.0.2",
  "private": true,
  "description": "",
  "main": "iac.js",
  "scripts": {
    "test": "mocha",
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.0.2",
    "@grucloud/provider-aws": "^2.0.2"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
