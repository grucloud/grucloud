{
  "name": "example-grucloud-ecs-simple",
  "version": "2.0.1",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.0.1",
    "@grucloud/provider-aws": "^2.0.1",
    "rubico": "^1.6.29"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
