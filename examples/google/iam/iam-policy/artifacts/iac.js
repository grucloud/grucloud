// Generated by gcp2gc
const { get } = require("rubico");
const { GoogleProvider } = require("@grucloud/provider-google");

const createResources = ({ provider }) => {
  provider.iam.makeServiceAccount({
    name: get("config.iam.ServiceAccount.saSaExamplePolicy.name"),
    properties: get("config.iam.ServiceAccount.saSaExamplePolicy.properties"),
  });

  provider.iam.makePolicy({
    name: get("config.iam.Policy.policy.name"),
    properties: get("config.iam.Policy.policy.properties"),
  });
};

exports.createResources = createResources;

exports.createStack = async ({ createProvider }) => {
  const provider = createProvider(GoogleProvider, {
    config: require("./config"),
  });
  createResources({
    provider,
  });

  return {
    provider,
  };
};
