{
  "name": "@grucloud/example-google-dns-github-page",
  "version": "1.16.0",
  "private": true,
  "description": "setup dns for github page",
  "main": "index.js",
  "scripts": {
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.16.0",
    "@grucloud/provider-google": "^1.16.0"
  }
}
