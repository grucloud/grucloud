{
  "name": "@grucloud/example-google-website-https",
  "version": "2.0.0",
  "description": "",
  "private": true,
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.0.0",
    "@grucloud/provider-google": "^2.0.0",
    "mime-types": "^2.1.27",
    "rubico": "^1.6.29",
    "shelljs": "^0.8.4"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
