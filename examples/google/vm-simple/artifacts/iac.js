// Generated by gcp2gc
const { get } = require("rubico");
const { GoogleProvider } = require("@grucloud/provider-google");

const createResources = ({ provider }) => {
  provider.compute.makeFirewall({
    name: get("config.compute.Firewall.defaultAllowIcmp.name"),
    properties: get("config.compute.Firewall.defaultAllowIcmp.properties"),
  });

  provider.compute.makeFirewall({
    name: get("config.compute.Firewall.defaultAllowInternal.name"),
    properties: get("config.compute.Firewall.defaultAllowInternal.properties"),
  });

  provider.compute.makeFirewall({
    name: get("config.compute.Firewall.defaultAllowRdp.name"),
    properties: get("config.compute.Firewall.defaultAllowRdp.properties"),
  });

  provider.compute.makeFirewall({
    name: get("config.compute.Firewall.defaultAllowSsh.name"),
    properties: get("config.compute.Firewall.defaultAllowSsh.properties"),
  });

  provider.compute.makeVmInstance({
    name: get("config.compute.VmInstance.webServer.name"),
    properties: get("config.compute.VmInstance.webServer.properties"),
  });
};

exports.createResources = createResources;

exports.createStack = async ({ createProvider }) => {
  const provider = createProvider(GoogleProvider, {
    config: require("./config"),
  });
  createResources({
    provider,
  });

  return {
    provider,
  };
};
