{
  "name": "grucloud-k8s-starhackit",
  "version": "1.0.0",
  "description": "deploying starhackit on kubernetes",
  "main": "index.js",
  "scripts": {
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o",
    "link": "npm link @grucloud/core"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "*"
  }
}
