{
  "name": "@grucloud/example-k8s-service-account-aws-role",
  "version": "2.3.0",
  "private": true,
  "description": "link a service account to an AWS role",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d-f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.3.0",
    "@grucloud/provider-aws": "^2.3.0",
    "@grucloud/provider-k8s": "^2.3.0",
    "rubico": "^1.6.21"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
