{
  "name": "@grucloud/example-k8s-service-account-aws-role",
  "version": "11.1.0",
  "private": true,
  "description": "link a service account to an AWS role",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d-f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^11.1.0",
    "@grucloud/provider-aws": "^11.1.0",
    "@grucloud/provider-k8s": "^11.1.0",
    "rubico": "1.9.6"
  },
  "devDependencies": {
    "mocha": "10.2.0",
    "mocha-junit-reporter": "2.2.0"
  }
}
