{
  "name": "@grucloud/example-multi",
  "version": "3.6.1",
  "private": true,
  "description": "Run multiple GruCloud examples",
  "main": "iac.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d -f && gc l && gc a -f && gc a && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^3.6.0",
    "@grucloud/provider-aws": "^3.6.0",
    "@grucloud/provider-azure": "^3.6.1",
    "@grucloud/provider-google": "^3.6.0",
    "@grucloud/provider-k8s": "^3.6.0",
    "@grucloud/provider-scaleway": "^3.6.0"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
