{
  "name": "@grucloud/example-multi",
  "version": "10.0.2",
  "private": true,
  "description": "Run multiple GruCloud examples",
  "main": "iac.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d -f && gc l && gc a -f && gc a && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^10.0.2",
    "@grucloud/provider-aws": "^10.0.2",
    "@grucloud/provider-azure": "^10.0.2",
    "@grucloud/provider-google": "^10.0.2",
    "@grucloud/provider-k8s": "^10.0.2",
    "@grucloud/provider-scaleway": "^10.0.2"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
