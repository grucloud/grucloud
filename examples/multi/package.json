{
  "name": "@grucloud/example-multi",
  "version": "2.3.1",
  "private": true,
  "description": "Run multiple GruCloud examples",
  "main": "iac.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -f && gc d -f && gc l && gc a -f && gc a && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.3.1",
    "@grucloud/provider-aws": "^2.3.1",
    "@grucloud/provider-azure": "^2.3.1",
    "@grucloud/provider-google": "^2.3.1",
    "@grucloud/provider-k8s": "^2.3.1",
    "@grucloud/provider-scaleway": "^2.3.1"
  },
  "devDependencies": {
    "mocha": "^9.0.3"
  }
}
