{
  "name": "@grucloud/example-azure",
  "version": "2.0.2",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "graph-target": "gc graph",
    "graph-live": "gc list --graph",
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^2.0.2",
    "@grucloud/provider-azure": "^2.0.2",
    "ping": "^0.4.1",
    "rubico": "^1.7.3",
    "ssh2": "^1.3.0"
  },
  "devDependencies": {
    "mocha": "^9.1.0"
  }
}
