{
  "name": "@grucloud/example-module-k8s-redis",
  "version": "1.10.20",
  "description": "Grucloud example for the k8s redis module",
  "main": "iac.js",
  "private": true,
  "scripts": {
    "test": "mocha --exit './{,!(node_modules)/**}/*.test.js'",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.10.20",
    "@grucloud/module-k8s-redis": "^1.10.20",
    "@grucloud/provider-k8s": "^1.10.20",
    "rubico": "^1.6.21"
  },
  "devDependencies": {
    "mocha": "^8.3.2"
  }
}
