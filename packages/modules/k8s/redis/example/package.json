{
  "name": "@grucloud/example-module-k8s-redis",
  "version": "12.10.2",
  "description": "Grucloud example for the k8s redis module",
  "main": "iac.js",
  "private": true,
  "scripts": {
    "graph-target": "gc graph --title 'Redis Module'",
    "graph-live": "gc list --graph",
    "test": "mocha",
    "e2e": "gc d -f && gc d && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^12.10.2",
    "@grucloud/module-k8s-redis": "^12.10.2",
    "@grucloud/provider-k8s": "^12.10.2",
    "rubico": "1.9.6"
  },
  "devDependencies": {
    "mocha": "10.2.0"
  }
}
