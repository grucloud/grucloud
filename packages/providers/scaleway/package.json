{
  "name": "@grucloud/provider-scaleway",
  "version": "1.10.1",
  "description": "The GruCloud Scaleway provider",
  "main": "ScalewayProvider.js",
  "scripts": {
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o",
    "link": "npm link @grucloud/core",
    "test": "NODE_PATH=. mocha  './test/**/*.test.js'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^1.10.1"
  },
  "devDependencies": {
    "mocha": "^8.3.2"
  }
}
