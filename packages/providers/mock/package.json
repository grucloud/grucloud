{
  "name": "@grucloud/provider-mock",
  "version": "7.4.0",
  "description": "The GruCloud Mock provider",
  "main": "MockProvider.js",
  "private": true,
  "scripts": {
    "e2e": "gc d -a -f && gc d -a && gc l && gc a -f && gc a && gc d -f && gc d && gc l -o",
    "test": "NODE_PATH=. mocha  './test/**/*.test.js'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@grucloud/core": "^7.4.0",
    "axios": "^0.26.0",
    "axios-mock-adapter": "^1.20.0",
    "bluebird": "^3.7.2",
    "rubico": "^1.8.9",
    "shelljs": "^0.8.5",
    "url-join": "^4.0.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "mocha": "^9.2.1",
    "mocha-junit-reporter": "^2.0.2",
    "prompts": "^2.4.2",
    "sinon": "^13.0.1"
  }
}
